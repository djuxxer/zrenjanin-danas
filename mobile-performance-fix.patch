diff -ruN '--exclude=node_modules' '--exclude=package-lock.json' '--exclude=.next' '--exclude=public' '--exclude=*.patch' '--exclude=tsconfig.tsbuildinfo' live20/package.json repo/package.json
--- a/package.json	2026-07-16 20:11:13.000000000 +0000
+++ b/package.json	2026-07-16 20:15:34.989515136 +0000
@@ -37,5 +37,10 @@
     "postcss": "^8.4.47",
     "tailwindcss": "^3.4.13",
     "typescript": "^5.6.3"
-  }
+  },
+  "browserslist": [
+    "defaults",
+    "not IE 11",
+    "not op_mini all"
+  ]
 }
diff -ruN '--exclude=node_modules' '--exclude=package-lock.json' '--exclude=.next' '--exclude=public' '--exclude=*.patch' '--exclude=tsconfig.tsbuildinfo' live20/src/components/home/hero-section.tsx repo/src/components/home/hero-section.tsx
--- a/src/components/home/hero-section.tsx	2026-07-16 20:11:13.000000000 +0000
+++ b/src/components/home/hero-section.tsx	2026-07-16 20:14:58.116287276 +0000
@@ -1,4 +1,5 @@
 import Link from 'next/link'
+import Image from 'next/image'
 import { ArticleCard } from '@/components/article/article-card'
 import { CATEGORY_LABELS, CATEGORY_COLORS } from '@/types'
 import { cn, timeAgo } from '@/lib/utils'
@@ -26,10 +27,14 @@
           href={`/vest/${main.slug}`}
           className="lg:col-span-2 group relative rounded-xl overflow-hidden block h-full"
         >
-          <img
+          <Image
             src={main.image_url}
             alt={main.image_alt}
-            className="absolute inset-0 w-full h-full object-cover transition-transform duration-500 group-hover:scale-105"
+            fill
+            priority
+            fetchPriority="high"
+            sizes="(max-width: 1024px) 100vw, 66vw"
+            className="object-cover transition-transform duration-500 group-hover:scale-105"
           />
           <div className="absolute inset-0 bg-gradient-to-t from-black/90 via-black/50 to-transparent" />
           <div className="absolute bottom-0 left-0 right-0 p-6 md:p-8">
@@ -57,10 +62,12 @@
               href={`/vest/${article.slug}`}
               className="group relative rounded-xl overflow-hidden block h-1/2"
             >
-              <img
+              <Image
                 src={article.image_url}
                 alt={article.image_alt}
-                className="absolute inset-0 w-full h-full object-cover transition-transform duration-500 group-hover:scale-105"
+                fill
+                sizes="(max-width: 1024px) 100vw, 33vw"
+                className="object-cover transition-transform duration-500 group-hover:scale-105"
               />
               <div className="absolute inset-0 bg-gradient-to-t from-black/85 via-black/30 to-transparent" />
               <div className="absolute bottom-0 left-0 right-0 p-4">
