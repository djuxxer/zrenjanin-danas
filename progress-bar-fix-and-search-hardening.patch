diff -ruN '--exclude=node_modules' '--exclude=package-lock.json' '--exclude=.next' '--exclude=public' '--exclude=*.patch' '--exclude=tsconfig.tsbuildinfo' live19/src/components/article/reading-progress.tsx repo/src/components/article/reading-progress.tsx
--- a/src/components/article/reading-progress.tsx	2026-07-16 19:50:26.000000000 +0000
+++ b/src/components/article/reading-progress.tsx	2026-07-16 20:06:57.455620751 +0000
@@ -44,7 +44,7 @@
   }, [targetId])
 
   return (
-    <div className="fixed top-0 left-0 right-0 z-40 h-1 bg-gray-200/50 dark:bg-gray-800/50">
+    <div className="fixed top-0 left-0 right-0 z-[60] h-1 bg-gray-200/50 dark:bg-gray-800/50">
       <div
         className="h-full bg-brand-red transition-[width] duration-150 ease-out"
         style={{ width: `${progress}%` }}
diff -ruN '--exclude=node_modules' '--exclude=package-lock.json' '--exclude=.next' '--exclude=public' '--exclude=*.patch' '--exclude=tsconfig.tsbuildinfo' live19/src/lib/articles.ts repo/src/lib/articles.ts
--- a/src/lib/articles.ts	2026-07-16 19:50:26.000000000 +0000
+++ b/src/lib/articles.ts	2026-07-16 20:07:46.276391678 +0000
@@ -194,11 +194,18 @@
 
 export async function searchArticles(query: string): Promise<Article[]> {
   const supabase = await createClient()
+
+  // Bezbednosna mera: karakteri koji imaju posebno značenje u PostgREST filter
+  // sintaksi (,()."*) se uklanjaju iz korisničkog unosa pre ubacivanja u .or() filter,
+  // da niko ne bi mogao da ubaci dodatne/izmenjene filter uslove kroz pretragu.
+  const safeQuery = query.replace(/[,()."'*]/g, '').trim()
+  if (!safeQuery) return []
+
   const { data, error } = await supabase
     .from('articles')
     .select(ARTICLE_SELECT)
     .eq('published', true)
-    .or(`title.ilike.%${query}%,excerpt.ilike.%${query}%`)
+    .or(`title.ilike.%${safeQuery}%,excerpt.ilike.%${safeQuery}%`)
     .order('published_at', { ascending: false })
 
   if (error || !data) return []
